import React from 'react';
import {Text, View} from 'react-native';

import Profile from '_templates/profile';

const ProfileScreens = props => {
  return <Profile {...props} />;
};

export default ProfileScreens;
