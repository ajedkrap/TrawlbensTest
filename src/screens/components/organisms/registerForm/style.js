import {StyleSheet} from 'react-native';
import color from '_theme/colors';

export default StyleSheet.create({
  container: {
    backgroundColor: color.white,
  },
  scrollCont: {flexGrow: 1, paddingHorizontal: 8, paddingTop: 24},
});
