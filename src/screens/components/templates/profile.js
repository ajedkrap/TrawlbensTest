import React from 'react';
import {Text, View} from 'react-native';
import ProfileNote from '_organisms/profileNote';

const Profile = props => {
  return <ProfileNote {...props} />;
};

export default Profile;
