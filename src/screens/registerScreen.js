import React from 'react';
import {Text, View} from 'react-native';

import Register from '_templates/register';

const RegisterScreen = props => {
  return <Register {...props} />;
};

export default RegisterScreen;
